#include <iostream>
#include <cstdio>
#include <queue>
using namespace std;

int main(int argc, char const *argv[])
{

    return 0;
}
#include <map> //映射
#include <vector>
#include <cmath>
#include <algorithm>
// 注意string和string.h是不同的头文件
#include <cstring>  //对应于旧C 头文件的std 版本
#include <string>   //是包装了std 的C++头文件，对应的是新的string 类
#include <string.h> //<string.h>是旧的C 头文件，基于char*的字符串处理；
#include <queue>
#include <stack>
#include <cctype>
#include <ctype.h> //用于判断单个字符类型
class Solution
{
public:
    string replaceSpaces(string &str)
    {
    }
};