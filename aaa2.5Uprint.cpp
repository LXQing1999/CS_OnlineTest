#include <iostream>
#include <cstdio>
using namespace std;

int main(int argc, char const *argv[])
{
    string s;
    while (cin >> s)
    {
        if (s == EOF)
            break;
        int len = s.size();
    }
    return 0;
}